package com.florent.location

import org.junit.Test

import org.junit.Assert.*

/**
 * Test unitaire local exécuté sur la machine de développement.
 */
class ExampleUnitTest {
    /**
     * Vérifie un calcul simple.
     */
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
